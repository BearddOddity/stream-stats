{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window capabilities",
  "windows": ["main", "joystick-reporting"],
  "permissions": [
    "core:default"
  ]
}
